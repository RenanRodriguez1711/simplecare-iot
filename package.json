{
  "name": "simplecare-iot",
  "version": "1.0.0",
  "private": true,
  "description": "Backend y dashboard municipal de SimpleCare IoT (alarmas personales para adultos mayores).",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "test:verbose": "node --test --test-reporter=spec \"tests/**/*.test.js\""
  },
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
